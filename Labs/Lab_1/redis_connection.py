def get_connection():

    connection = {}

    connection['REDIS_HOST'] = '****'
    connection['REDIS_PORT'] = *****
    connection['REDIS_USERNAME'] = 'default'
    connection['REDIS_PASSWORD'] = '**********'

    return connection
